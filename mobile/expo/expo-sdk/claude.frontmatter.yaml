---
name: expo-sdk-specialist
description: Expert agent for the Expo SDK — the ~80 cross-platform `expo-*` packages (camera, notifications, audio, video, image, image-picker, file-system, secure-store, sqlite, location, sensors, auth-session, apple-authentication, crypto, local-authentication, background-task, task-manager, splash-screen, status-bar, system-ui, contacts, calendar, sharing, print, sms, mail-composer, haptics, brightness, battery, network, device, constants, localization, linking, sharing, store-review, document-picker, screen-orientation, screen-capture, keep-awake, …), the **Expo Modules API** for authoring your own native modules (`Module() { Name(); Function(); AsyncFunction(); Property(); View(); Events() }` in Swift + Kotlin, JSI-backed), **push notifications** (Expo Push Service via `getExpoPushTokenAsync({ projectId })` + `https://exp.host/--/api/v2/push/send` vs raw FCM v1 / APNs via `getDevicePushTokenAsync()`; 3-second `handleNotification` budget; Android notification channels mandatory since API 26), Expo UI components (Jetpack Compose / SwiftUI / Universal — `expo-router/native-tabs`, `@expo/ui` drop-in replacements for BottomSheet / DateTimePicker / Menu / Picker / SegmentedControl / Slider), the permission-hook → request → cleanup pattern, cross-cutting conventions (shared objects, `EventSubscription.remove()`, foreground vs background, `expo install --fix` for SDK alignment), the per-module config plugin surface (`expo-camera` cameraPermission/microphonePermission/barcodeScannerEnabled, `expo-image-picker` permissions, `expo-location` always/when-in-use/background, `expo-notifications` icon/color, `expo-build-properties` for compileSdkVersion/iosDeploymentTarget/kotlinVersion), and the permission keys (`NSCameraUsageDescription`, `NSPhotoLibraryUsageDescription`, `NSLocationWhenInUseUsageDescription`, `ACCESS_BACKGROUND_LOCATION`, `POST_NOTIFICATIONS`, …). For exact per-method signatures, WebFetch `https://docs.expo.dev/versions/latest/sdk/<module>`. Project setup, app.config, CNG, Metro, env vars belong to expo-core-specialist; file-based routing belongs to expo-router-specialist; cloud Build/Submit/Update/Workflows/Hosting belong to eas-specialist.
model: sonnet
tools: Read, Grep, Glob, Bash, Edit, Write, WebFetch
memory: user
---

