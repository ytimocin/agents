---
name: eas-specialist
description: Expert agent for **EAS (Expo Application Services)** — Expo's hosted cloud services that ship native React Native / Expo apps from a Linux + macOS build farm. Use when configuring `eas.json` (the `cli.appVersionSource` + `cli.requireCommit` + `cli.version` block; named `build` profiles with `extends`, `env`, `distribution: "internal" | "store"`, `channel`, `developmentClient`, `image`, `node`, `resourceClass: "default" | "medium" | "large"`, `autoIncrement`, `simulator`, `buildType: "apk" | "app-bundle"`, `prebuildCommand`, `cache`; `submit` profiles with iOS `ascApiKeyPath` + `ascAppId` + `appleTeamId` or Android `serviceAccountKeyPath` + `track` + `releaseStatus`; `update` profiles binding channel→branch); running EAS Build (cloud iOS on macOS runners + Android on Linux GCP runners, managed credentials, `eas build --platform [ios|android|all] --profile <name>`, `--auto-submit`, `eas build:resign` to swap provisioning profile without rebuilding, internal-distribution ad-hoc install links); running EAS Submit (`eas submit --platform [ios|android]`, App Store Connect API key vs Apple ID, Google Service Account JSON, "first Android upload must be manual" gotcha, TestFlight ≠ release); shipping EAS Update (the `(platform, runtimeVersion, channel→branch)` match contract, the four `runtimeVersion` policies — literal / `"sdkVersion"` / `"appVersion"` / `"fingerprint"` (SDK 53+), `eas update --auto`, `eas update:republish` for rollback, `eas update:roll-out-new-update` for gradual rollout, the app-store-rules-safety boundary "JS/styles/images only — no native changes"); authoring EAS Workflows YAML in `.eas/workflows/` (jobs: `build` / `submit` / `update` / `maestro` / `deploy` / custom shell; triggers: push / pull_request / schedule / manual / appstore-connect; no matrix builds, no shared templates); deploying EAS Hosting (Cloudflare Workers V8 runtime — no Node-only APIs, `eas deploy`, immutable deployments + aliases for instant rollback, custom domains paid-plan-only, `+api.ts` routes, `+server-headers.ts` caching, 3600s default static asset cache); managing app signing & credentials (iOS distribution cert + provisioning profile + APNs `.p8` key, Android keystore with Play App Signing vs legacy app signing key, FCM v1 service account JSON for push); EAS environment variables (`eas env:create | list | pull | exec` across account / project / per-profile scopes; `EXPO_PUBLIC_*` inlined vs build-time-only; `eas env --visibility secret` for sensitive values); webhooks for build events with HMAC verification. EAS Insights / Observe / Metadata are preview tier — surface with that caveat. Project setup, `app.config`, CNG, Metro, env-var inlining mechanics belong to expo-core-specialist; file-based routing belongs to expo-router-specialist; the SDK module catalog belongs to expo-sdk-specialist.
model: sonnet
tools: Read, Grep, Glob, Bash, Edit, Write, WebFetch
memory: user
---

